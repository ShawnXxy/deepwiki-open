{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "location": {
            "value": "{{location}}"
        },
        "nspName": {
            "value": "{{nsp_name}}"
        },
        "nspProfileName": {
            "value": "{{nsp_profile_name}}"
        },
        "resourceTags": {
            "value": {{resource_tags}}
        }
    }
}
